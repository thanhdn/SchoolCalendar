/*var _commonLocation = {
  location: null,
  setLocation: function(_location) {
    this.location = _location;
  },
  redirect: function(targetRedirect) {
    if(typeof this.location != undefined) {
        this.location.path(targetRedirect);
    }
  }
};*/

/*
angular.module('commonLocationApp', ['ngRoute'])
.controller('commonLocationController', ['$scope', '$location', function($scope, $location) {
    $scope.redirect = function(targetRedirect) {
        $location.path(targetRedirect);
    };
}]);*/
/*var navGrade = {
    header  : _resourceBundle["grade"],
    menu    : [
        {label : _resourceBundle["list"],   url : "#/grades"},
        {label : _resourceBundle["import"], url : "#/import"}
    ]
};
// init captions and urls for teacher navigation
var navTeacher = {
    header  : _resourceBundle["teacher"],
    menu    : [
        {label : _resourceBundle["list"],   url : "#/teachers"},
        {label : _resourceBundle["import"], url : "#/import"}
    ]
};

var navClass = {
    header  : _resourceBundle["class"],
    menu    : [
        {label : _resourceBundle["list"],   url : "#/teachers"},
        {label : _resourceBundle["import"], url : "#/import"}
    ]
};

var navCourse = {
    header  : _resourceBundle["course"],
    menu    : [
        {label : _resourceBundle["list"],   url : "#/teachers"},
        {label : _resourceBundle["import"], url : "#/import"}
    ]
};

var navSchedule = {
    header  : _resourceBundle["schedule"],
    menu    : [
        {label : _resourceBundle["calendar"],   url : "#/teachers"},
        {label : _resourceBundle["arrange"],    url : "#/import"}
    ]
};*/

/*
"navGrade" : {
    "header"  : "Khối học",
        "menu"    : [
        {"label" : "Danh sách",   "url" : "#\/grades"},
        {"label" : "Nhập từ excel", "url" : "#\/import"}
    ]
},
// init captions and urls for teacher navigation
"navTeacher" : {
    "header"  : "Giáo viên",
        "menu"    : [
        {"label" : "Danh sách",   "url" : "#\/grades"},
        {"label" : "Nhập từ excel", "url" : "#\/import"}
    ]
},

"navClass" : {
    "header"  : "Lớp học",
        "menu"    : [
        {"label" : "Danh sách",   "url" : "#\/grades"},
        {"label" : "Nhập từ excel", "url" : "#\/import"}
    ]
},

"navCourse" : {
    "header"  : "Môn học",
        "menu"    : [
        {"label" : "Danh sách",   "url" : "#\/grades"},
        {"label" : "Nhập từ excel", "url" : "#\/import"}
    ]
},

"navSchedule" : {
    "header"  : "Thời khóa biểu",
        "menu"    : [
        {"label" : "Lịch",                  "url" : "#\/grades"},
        {"label" : "Sắp xếp thời khóa biểu", "url" : "#\/import"}
    ]
}*/
/*function loadResource() {
       return new Promise(function(resolve, reject) {
           ngI18nResourceBundle.get({locale: "vi"}).success(function (resourceBundle) {
               return resolve(resourceBundle);
           });
       });
   };*/